{% extends 'base_dashboard.html.twig' %}

{% block title %}Keyword{% endblock %}

{% block body %}
    <h1>Keyword</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ keyword.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ keyword.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ keyword.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dashboard_keyword_index') }}">back to list</a>

    <a href="{{ path('dashboard_keyword_edit', {'id': keyword.id}) }}">edit</a>

    {{ include('dashboard/keyword/_delete_form.html.twig') }}
{% endblock %}
